gfbo:
  sftp:
    host: ${host}
    port: ${port}
    user: ${user}
    allow-unknown-keys: true
    private-key: ${private-key}
    private-key-passphrase: ""
    base-remote-directory: ${base-remote-directory}